# 로지스틱 성장 + 수확 모델 — 수업용 이론 정리

시스템생물학 TA 수업 자료. [index.html](index.html) 인터랙티브 사이트와 함께 사용하도록 구성했습니다.
학생들에게는 "왜 평형점이 사라지는 방식이 두 가지로 다른가"를 붕괴(collapse)의 **경고 유무**라는
직관적 질문으로 던진 뒤, 분기 이론으로 답을 주는 순서를 권장합니다.

---

## 1. 왜 이 모델을 가르치는가

로지스틱 성장 + 수확 모델은 생물수학에서 가장 단순한 축에 속하지만, 시스템생물학 전반에 등장하는
핵심 개념 세 가지를 한 번에 보여줍니다.

1. **비선형 동역학의 평형점과 안정성** — 유전자 회로, 신호전달 네트워크의 정상상태 분석과 동일한 기법(선형화, f'(N*)의 부호).
2. **분기(bifurcation)** — 파라미터가 조금만 바뀌어도 시스템의 정성적 행동(정상상태의 개수·안정성)이 바뀌는 현상. Toggle switch, 세포 주기 스위치, apoptosis on/off 등 양성 되먹임 회로의 **쌍안정성(bistability)**·**이력현상(hysteresis)**이 전부 안장-마디 분기(saddle-node bifurcation)의 한 형태.
3. **선형 안정성 vs 구조적 안정성** — "지금 안정하다"와 "조금만 조건이 바뀌어도 안정하다"는 다른 질문이라는 것. 정량 수확 모델은 **경고 없는 붕괴**를 통해 이 차이를 극적으로 보여줌.

즉 이 모델은 "물고기 개체수" 얘기처럼 보이지만, 실제로는 **양성 되먹임이 있는 모든 생물학적 스위치**(세포 분화 결정, 락토스 오페론의 히스테리시스, 심장세포의 흥분성 등)에 그대로 적용되는 수학 구조를 가르치는 도구입니다.

---

## 2. 로지스틱 성장 기초

Verhulst(1838) 모델:

```
dN/dt = rN(1 − N/K)
```

- `N`: 개체수(또는 세포 수, mRNA 농도 등 — 시스템생물학에서는 개체수 대신 임의의 성분 농도로 일반화 가능)
- `r`: 내재적 증가율 (per-capita growth rate at low density)
- `K`: 환경 수용력 (carrying capacity)

**1인당 성장률** `(1/N)(dN/dt) = r(1 − N/K)`는 N에 대해 선형이며, N=0에서 r로 시작해 N=K에서 0이 되는
직선입니다. 이 직선이 이후 수확항에 의해 어떻게 변형되는지가 이 모델의 핵심 관찰 포인트입니다.

수확 없는 로지스틱 방정식은 해석해가 존재합니다 (검증용):

```
N(t) = K·N₀·e^(rt) / (K + N₀·(e^(rt) − 1))
```

---

## 3. 수확항 추가: 두 가지 방식과 그 차이

| | 비례 수확 (constant effort) | 정량 수확 (constant yield) |
|---|---|---|
| 방정식 | `dN/dt = rN(1−N/K) − hN` | `dN/dt = rN(1−N/K) − H` |
| 수확량 | 개체수에 **비례** (어획 노력 h 고정) | 개체수와 **무관하게 일정** (쿼터 H 고정) |
| f(N) 그래프 변화 | 포물선의 기울기(원점 근처 접선 r)가 h만큼 줄어듦 | 포물선 전체가 H만큼 아래로 평행이동 |
| 생물학적 유비 | 사냥/어업의 "노력 기반" 규제, 분해효소 농도에 비례하는 분해 반응 | 고정 용량의 소비/제거 장치, 효소가 포화된(0차) 반응 속도 |

이 표는 학생들에게 "같은 '수확'이라도 메커니즘이 다르면 분기 유형 자체가 바뀐다"는 것을
먼저 각인시키기 위한 것입니다. 다음 절에서 왜 그런지 수학적으로 보입니다.

---

## 4. 평형점과 안정성 분석

일반화된 형태 `dN/dt = f(N)`에서 평형점은 `f(N*) = 0`, 안정성은 `f'(N*)`의 부호로 판정합니다
(`f'(N*) < 0` → 안정, `f'(N*) > 0` → 불안정). 이것이 시스템생물학 정상상태 분석에서 쓰는
선형화(linearization)와 정확히 동일한 절차임을 강조하세요.

### 4.1 비례 수확 — 초임계 분기 (transcritical bifurcation)

```
f(N) = N·((r − h) − (r/K)N)
```

| 평형점 | 안정성 조건 |
|---|---|
| `N = 0` | h < r이면 불안정, h > r이면 안정 |
| `N* = K(1 − h/r)` | h < r이면 안정, h > r이면 불안정 (h>r에서는 N*<0이라 생물학적으로 무의미) |

`h* = r`에서 두 평형점이 **충돌하며 안정성을 맞바꿉니다** — 이것이 초임계 분기의 정의적 특징입니다.
h를 늘리면 안정 평형 `N*`가 **연속적으로** 0을 향해 줄어들다가 h=r에서 0과 만납니다.
→ 붕괴 전에 개체수 감소라는 **관측 가능한 경고**가 있음.

지속 가능 최대 수확량(MSY): `Y(h) = hK(1−h/r)`, `h=r/2`에서 최댓값 `rK/4`.

### 4.2 정량 수확 — 안장-마디 분기 (saddle-node bifurcation)

```
f(N) = −(r/K)N² + rN − H
```

이차방정식이므로 판별식으로 평형점 개수가 결정됩니다.

```
N± = (K/2)(1 ± √(1 − 4H/rK))
```

| 조건 | 평형점 | 안정성 |
|---|---|---|
| H < rK/4 | N+, N− 두 개 | N+ 안정, N− 불안정(멸종 문턱, separatrix) |
| H = rK/4 | N+ = N− = K/2 하나 | 반안정(semi-stable) — 분기점 |
| H > rK/4 | 없음 | 모든 초기값에서 유한 시간 내 N→0으로 붕괴 |

핵심은 H가 H\*=rK/4에 도달하는 **순간까지 안정 평형 N+는 거의 변하지 않다가**(K/2 근처에서
평평), H\*를 넘는 순간 평형점 자체가 통째로 사라진다는 것 — **경고 없는 붕괴(catastrophic
collapse)**. 이것이 안장-마디 분기의 특징이며, 실제 어업 붕괴(대구 어장 등)의 표준 설명 모델입니다.

### 4.3 두 분기의 대응 (표로 대비)

| | 초임계 (비례 수확) | 안장-마디 (정량 수확) |
|---|---|---|
| 붕괴 전 경고 | 있음 (평형 개체수가 서서히 감소) | 없음 (평형 개체수가 거의 안 변하다 사라짐) |
| 히스테리시스 | 없음 (h를 줄이면 바로 회복) | **있음** — H를 다시 줄여도 이미 0으로 간 개체군은 자동 회복 안 됨 |
| f(N)=0의 근 개수 변화 | 두 근이 자리를 바꿈(교환) | 두 근이 하나로 합쳐졌다 소멸 |
| 시스템생물학 유비 | 유전자 발현의 점진적 dose-response | 양성 되먹임 스위치의 쌍안정성/이력현상 (예: Xenopus 난모세포 성숙, 세포주기 체크포인트) |

**강조할 점**: 정량 수확의 히스테리시스가 시스템생물학에서 훨씬 더 자주 마주치는 패턴입니다.
일단 스위치가 꺼지면(H>H\* 이후 N→0), H를 다시 낮춰도 이미 N=0에 있으므로 자발적으로 복구되지
않습니다 — 되돌리려면 외부에서 N을 직접 재도입(재도입 개체, 또는 세포 신호 리셋)해야 합니다.

---

## 5. Maximum Sustainable Yield (MSY)와 관리적 함의

두 모델 모두 `H*` 또는 최대 지속가능수확량이 **동일한 값 rK/4**(포물선 꼭짓점 높이)에서 발생합니다.
그러나:

- 비례 수확에서는 h=r/2(즉 MSY 지점)에서 조업해도 **평형이 여전히 존재**(K/2에서 안정).
- 정량 수확에서는 H=H\*가 **평형점이 사라지는 정확한 경계**이므로, MSY 근처에서 조업하는 것 자체가
  구조적으로 위험합니다 — 파라미터 추정 오차나 환경 변동(r 또는 K의 변화)만으로도 순식간에 H>H*가
  될 수 있음.

이 차이는 실제 수산자원관리에서 "허용어획량을 노력(effort) 기반으로 규제할 것인가, 쿼터(quota) 기반으로
규제할 것인가"라는 정책 논쟁과 직결됩니다. 학생들에게 이 정책적 함의까지 연결하면 흥미를 끌기 좋습니다.

---

## 6. 수업 시연 순서 제안 (사이트 활용 가이드)

`index.html`의 프리셋 6개를 아래 순서로 시연하는 것을 권장합니다.

1. **수확 없음** — 순수 로지스틱, f(N) 포물선과 평형 K를 확인.
2. **지속 가능** (비례 수확, h < r) — 평형이 K보다 낮아졌지만 안정적임을 확인.
3. **초임계 분기 직전** (h → r⁻) — 슬라이더로 h를 천천히 올리며 평형 개체수가 **서서히** 0에 가까워지는
   것을 실시간으로 보여줌. "경고가 보인다"를 학생들이 직접 관찰하게 함.
4. **수확 과다 → 멸종** (h > r) — 평형이 사라지고 N=0만 안정임을 확인. 분기 다이어그램에서 X자 교차를 확인.
5. **MSY 근처** (정량 수확, H ≈ H*) — 슬라이더로 H를 아주 조금만 올려도 평형점 개수가 2→1→0으로
   급변하는 것을 시연. 시간 궤적 그래프에서 분리선(separatrix) 아래로 출발한 궤적이 유한 시간 내 붕괴하는
   것을 강조.
6. **안장-마디 이후 붕괴** (H > H*) — 어떤 초기값에서도 회복 불가능함을 보여주고, 다시 H를 낮춰도
   (개체수가 이미 0이므로) 회복되지 않는다는 히스테리시스를 설명.

시연 후 분기 다이어그램(가로: 수확 강도, 세로: 평형 개체수)에서 두 모드의 곡선 모양 차이
(X자 교차 vs 접힘)를 나란히 비교하며 마무리하면 좋습니다.

---

## 7. 학생 토론/과제 질문 (안)

1. 두 수확 모델에서 `h* = r`과 `H* = rK/4`가 **같은 물리량**(최대 지속가능수확량 rK/4)을 가리키는데도
   왜 붕괴 양상이 다른가? f(N)의 그래프 모양(선형 기울기 변화 vs 수직 이동)으로 설명하라.
2. 안장-마디 분기가 나타나는 다른 생물학적 시스템을 하나 찾아 이 모델과 대응시켜라 (힌트: 세포 주기
   스위치, apoptosis, 신경세포 발화 역치).
3. 비례 수확 모델에서 h를 r*/2보다 살짝 넘겨 조업하다가, 위험을 깨닫고 즉시 h를 다시 낮췄다고 하자.
   개체군은 회복되는가? 정량 수확 모델에서 H가 H*를 살짝 넘긴 경우와 비교하라. (히스테리시스 여부)
4. (선택, 심화) `r`과 `K`가 계절적으로 진동한다면(즉 시간에 따라 변하는 파라미터), 안장-마디 분기점
   H*(t)=r(t)K(t)/4도 함께 진동한다. 고정된 쿼터 H로 조업할 때 어떤 위험이 생기는가?

---

## 8. 참고문헌 / 추가 자료

- Verhulst, P.F. (1838). *Notice sur la loi que la population suit dans son accroissement*.
- Strogatz, S.H. *Nonlinear Dynamics and Chaos* — 1차원 흐름, 분기 이론 표준 교재 (2장, 3장).
- Clark, C.W. *Mathematical Bioeconomics* — 수확 모델과 MSY의 표준 참고서.
- 시스템생물학 연결: Ferrell, J.E. 관련 논문들 — 양성 되먹임과 쌍안정성/히스테리시스 (Xenopus 난모세포
  성숙 등)의 고전적 예시.

---

## 9. 3D 확장 — 접힘 곡면과 진짜 쌍안정성 ([3d.html](3d.html))

2D 사이트는 파라미터 하나(H 또는 h)만 움직이며 평형점을 관찰합니다. `3d.html`은 **두 번째 파라미터**를
추가로 축에 올려, 평형 개체수 N\*을 곡면의 높이로 그립니다 — 안장-마디 분기점이 곡면이 접히는
능선(fold curve)으로 시각화됩니다.

### 9.1 접힘 곡면 (기본 모델의 3D 확장)

`dN/dt = rN(1−N/K) − H`에서 K까지 축으로 두면 (H, K, N\*) 3차원 곡면이 됩니다. K를 고정한 단면이
정확히 2D 사이트의 분기 다이어그램과 같은 곡선입니다. 분기점 `H\*=rK/4`가 원점을 지나는 직선을
그리며, 제어변수 평면(H–K)에는 이 직선 아래(2개 평형) / 위(0개 평형)로 나뉘는 단순한 쐐기 모양이
나타납니다.

### 9.2 왜 처음 시도(정량 수확 + 알리 효과)가 실패했는가

알리 효과(Allee effect, 개체수가 임계값 A 아래로 떨어지면 성장률 자체가 음이 되는 효과)를 더한
`dN/dt = rN(1−N/K)(N/A−1) − H`로 "컵 파국(cusp catastrophe)"을 그리려 했으나, **정량 수확(−H)은
N=0에서도 개체를 계속 제거하려 하기 때문에 H>0이면 N=0 자체가 평형이 아닙니다.** 그 결과 평형 3개가
성립하는 지점은 H=0인 단 한 점뿐이고, 열린 구간에서의 진짜 쌍안정성은 나타나지 않습니다 — 수학적으로
검증된 구조적 결과이지, 구현 버그가 아니었습니다.

### 9.3 알리 효과 + 비례 수확 → 진짜 쌍안정성

비례 수확(−hN)으로 바꾸면 N=0에서 수확항도 정확히 0이 되어, **N=0(멸종)이 h와 무관하게 항상 안정
평형으로 남습니다.**

```
dN/dt = rN(1−N/K)(N/A−1) − hN
```

N=0을 제외한 나머지 두 평형(불안정 문턱, 그 위의 안정 평형)은 `h* (A) = r(1−A/K)²/(4A/K)`에서
접혀 사라집니다 — 이 값을 넘는 순간 개체군은 유일하게 남은 안정 평형인 N=0으로 떨어지고, 이후 h를
다시 낮춰도 이미 0에 있으므로 저절로 회복되지 않습니다. **진짜 이력현상(hysteresis)이자, 유전자
스위치·세포 운명 결정과 구조적으로 대응되는 쌍안정성**입니다 (N=0이 "항상 존재하는 안정 상태", 접혀
사라지는 나머지 한 쌍이 "임계값-생존 평형" 쌍에 해당). 제어변수 평면(h–A)에서는 A가 커질수록(알리
효과가 강할수록) 안전 마진 h\*(A)가 좁아지는 쐐기 모양으로 나타나며, A→0(알리 효과 없음)에서 h\*가
가장 넓고, A→K에 가까워질수록 0으로 좁아집니다.

이 대비(9.2 vs 9.3) 자체가 좋은 수업 소재입니다 — **같은 생물학적 효과(알리 효과)라도 수확 방식(정량
vs 비례)에 따라 전혀 다른 결론**(쌍안정성이 사실상 안 보인다 vs 뚜렷한 이력현상)이 나온다는 것을
직접 계산으로 확인하는 사례이기 때문입니다.

---

## 부록: 검증 데이터 (README.md에서 재인용)

- RK4 궤적 vs 해석해(수확 없음): 최대 오차 `1.2e-7`
- 비례 h=0.3, r=0.6, K=1000 → 평형 500, RK4 수렴값 500.000
- 정량 H=140 (H*=150) → 평형 370.9 / 629.1, 문턱 위에서 출발 시 629.1로 수렴, 아래에서 출발 시 0으로 붕괴
- H=165 > H* → 평형점 0개, 모든 초기값이 0으로 붕괴
